local single_command_snippet = {}
local single_command_specs = {
    sec = {
        context = {
            name = "section",
            dscr = "add a section"
        },
        command = [[\section]]
    },
    ssec = {
        context = {
            name = "section",
            dscr = "add a section"
        },
        command = [[\subsection]]
    }
}
